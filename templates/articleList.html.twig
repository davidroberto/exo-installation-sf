<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <main>
        {# si l'age envoyé par le controleur est supérieur à 18 #}
        {% if age > 18 %}
            {# alors j'affiche tous les articles #}
            {% for article in articles %}
                <h1>{{ article.titre }}</h1>
                <p>{{ article.contenu }}</p>
                <img src="{{ article.image }}" alt="">
            {% endfor %}
        {% else %}
            <p>tu n'as pas le droit</p>
        {% endif %}

    </main>
</body>
</html>
{# je fais une condition sur la variable display (qui contient
 un booleen #}
{% if display %}
    {# si la variable display est true, j'affiche la valeur de la variable age #}
    {{ age }}
{% endif %}